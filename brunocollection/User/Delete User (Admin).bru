meta {
  name: Delete User (Admin)
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/users/{{userId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

docs {
  Delete a user by ID.
  Only accessible to ADMIN role.
}
