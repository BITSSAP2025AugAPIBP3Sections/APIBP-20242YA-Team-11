meta {
  name: Confirm Payment
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/api/v1/payments/{{orderId}}/confirm
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

docs {
  Confirm that a payment has been successfully processed for an order.
  Requires authentication.
}
