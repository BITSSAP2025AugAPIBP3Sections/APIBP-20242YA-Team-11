meta {
  name: Remove Item from Cart
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/cart/items/{{cartItemId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

docs {
  Remove a specific item from the shopping cart by item ID.
  Requires CUSTOMER role.
}
